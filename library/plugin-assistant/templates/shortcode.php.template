<?php
/**
 * Shortcode: [{{tag}}]
 *
 * {{description}}
 *
 * @package Plugin_Shortcode
 */

// Prevent direct access.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Register the [{{tag}}] shortcode.
 *
 * @param array  $atts    Shortcode attributes.
 * @param string $content Shortcode content (if enclosing).
 * @return string Shortcode output.
 */
function {{prefix}}_{{tag}}_shortcode( $atts{{content_param}} ): string {
	$atts = shortcode_atts(
		array(
			{{defaults}}
		),
		$atts,
		'{{tag}}'
	);
{{content_var}}
	// Start output buffering.
	ob_start();
	?>
	<div class="{{prefix}}-{{tag}}">
		<!-- Shortcode output here -->
		<p><?php esc_html_e( '{{description}}', '{{prefix}}' ); ?></p>
	</div>
	<?php
	return ob_get_clean();
}
add_shortcode( '{{tag}}', '{{prefix}}_{{tag}}_shortcode' );
